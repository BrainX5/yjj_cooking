# Forest 项目讲解说明

## 1. 项目概述

这是一个基于 Unity 的小型交互演示项目，当前核心内容是一个“蘑菇汤制作”玩法场景。  
玩家通过键盘操作控制火力、投放蘑菇、进行搅拌，按步骤完成一锅蘑菇汤。

项目更偏向“教学演示”和“场景交互展示”，不是完整商业游戏框架。它的特点是：

- 用很少的脚本完成一个完整的交互闭环
- 运行时自动搭建玩法需要的场景元素和 UI
- 支持复用现有场景资源，也支持在空场景中生成基础演示环境

## 2. 开发环境

- Unity 版本：`2022.3.62f3c1`
- 解决方案文件：`Forest.sln`
- 主要依赖：
  - `com.unity.ugui`
  - `com.unity.textmeshpro`
  - `com.unity.timeline`
  - `com.unity.visualscripting`

说明：
当前实际核心 UI 脚本使用的是 `UnityEngine.UI`，也就是传统 UGUI。

## 3. 项目目录重点

当前项目里真正和玩法相关的核心目录主要是：

- `Assets/Scenes/SampleScene.unity`
  - 当前示例场景
- `Assets/Scripts/Cooking/MushroomSoupSceneBootstrap.cs`
  - 场景初始化脚本，负责自动创建锅、火、UI、搅拌勺、蘑菇投放点等
- `Assets/Scripts/Cooking/MushroomSoupGame.cs`
  - 玩法主逻辑脚本，负责输入、火力变化、烹饪进度、阶段切换、动画和 UI 刷新

其余如下面这些目录，主要是素材资源：

- `Assets/3D Game Kit Clay Pot`
  - 锅模型资源
- `Assets/Oode studios/Lowpoly nature`
  - 蘑菇、自然环境、火堆等低模素材
- `Assets/DenysAlmaral/FishAlive`
  - 鱼类演示资源，目前不是本玩法的核心部分
- `Assets/FinottiGames/LowPoly Trees Game-Ready Free-Asset`
  - 树木资源，目前主要用于场景补充

## 4. 项目运行逻辑

### 4.1 整体流程

项目启动后，`MushroomSoupSceneBootstrap` 会先执行：

1. 检查场景里是否已经存在 `MushroomSoupGame`
2. 如果没有，就自动构建当前玩法需要的对象
3. 创建或复用火焰特效、锅、汤面、搅拌勺、蘑菇动画模板
4. 创建屏幕 UI
5. 动态挂载 `MushroomSoupGame`，把所有引用传进去

之后由 `MushroomSoupGame` 接管游戏循环，在 `Update()` 中持续处理：

- 玩家输入
- 火力衰减
- 烹饪进度推进
- 搅拌动画
- 蘑菇投放动画
- 汤面缩放效果
- UI 文本和进度条刷新

### 4.2 玩法阶段

蘑菇汤制作过程被拆成多个阶段：

1. `HeatingToQuarter`
   - 先加热到 1/4 进度
2. `NeedMushroom`
   - 到 1/4 后暂停，等待玩家加蘑菇
3. `HeatingToHalf`
   - 加完蘑菇后继续加热到 1/2
4. `NeedFirstStir`
   - 到 1/2 后暂停，等待第一次搅拌
5. `HeatingToThreeQuarters`
   - 搅拌后继续加热到 3/4
6. `NeedSecondStir`
   - 到 3/4 后暂停，等待第二次搅拌
7. `HeatingToDone`
   - 第二次搅拌后继续加热到完成
8. `Completed`
   - 蘑菇汤制作完成

这套状态机是整个玩法逻辑的核心。

## 5. 玩家操作说明

当前按键设计如下：

- `Space`
  - 增加火力
- `UpArrow` 或 `DownArrow`
  - 投放蘑菇
- `LeftArrow` + `RightArrow`
  - 完成一次搅拌

补充说明：

- 火力不是永久增加，会随时间自动衰减
- 搅拌不是按一个键就完成，而是左右键都要按到
- 蘑菇只能在指定阶段加入，非正确阶段按键不会推进流程

## 6. 两个核心脚本说明

## 6.1 `MushroomSoupSceneBootstrap.cs`

这个脚本主要负责“搭建场景”和“连接引用”。

它的作用包括：

- 检测场景中是否已经存在玩法对象
- 选择复用现有环境，还是创建一套基础环境
- 查找已有火焰粒子特效
- 创建锅和汤面
- 创建搅拌勺
- 创建蘑菇的出生点和落点
- 构建 UI Canvas、文本和滑条
- 最后创建 `MushroomSoupGame`

几个比较重要的配置字段：

- `useExistingSceneEnvironment`
  - 是否优先使用场景里已经存在的环境
- `anchorToExistingFire`
  - 是否尝试依附已有火焰位置
- `potPrefabPath`
  - 锅 prefab 的资源路径
- `mushroomPrefabPath`
  - 蘑菇 prefab 的资源路径

说明：
这个脚本在编辑器环境下会通过 `AssetDatabase` 和 `PrefabUtility` 加载资源，所以它非常适合快速做演示原型。

## 6.2 `MushroomSoupGame.cs`

这个脚本是玩法主控。

它管理的内容包括：

- 当前烹饪阶段
- 火力值变化
- 烹饪进度变化
- 已投放蘑菇数量
- 搅拌输入判定
- 锅、汤面、火焰、勺子的表现
- UI 提示内容

主要逻辑可以理解成下面几块：

### 输入处理

- `Space` 提升火力
- `Up/Down` 尝试加蘑菇
- `Left/Right` 记录搅拌输入

### 数值推进

- 火力会根据 `fireDecayPerSecond` 自动衰减
- 烹饪进度会根据 `firePower * maxCookRatePerSecond` 推进
- 只有在可加热阶段，进度才会继续增长

### 阶段阻塞

当到达关键节点后，系统会暂停进度推进，直到玩家完成对应动作：

- 25% 时等加蘑菇
- 50% 时等第一次搅拌
- 75% 时等第二次搅拌

### 视觉反馈

- 火力越大，火焰粒子越强
- 锅会有轻微缩放反馈
- 搅拌时勺子会绕圈摆动
- 蘑菇会从投放点飞入锅中
- 汤的颜色会随着阶段推进改变
- 汤面会在搅拌和投料时产生放大波动

## 7. UI 说明

UI 是运行时动态生成的，不依赖预制好的 Canvas。

当前界面包含：

- 标题：`蘑菇汤`
- 提示文字：告诉玩家当前应该做什么
- 火力数值
- 烹饪进度百分比
- 已加蘑菇数量
- 火力滑条
- 进度滑条

优点：

- 场景里不需要手动提前摆 UI
- 脚本自包含，拷到新场景也能直接工作

需要注意：

- 当前使用的是传统 `Text` 组件，不是 TMP
- 字体优先尝试系统中文字体，如 `Microsoft YaHei`

## 8. 资源复用机制

当前项目不是完全从零生成所有模型，而是“代码生成 + 现有资源复用”的混合方式。

例如：

- 锅模型优先从 `potPrefabPath` 指向的 prefab 加载
- 蘑菇模型优先从 `mushroomPrefabPath` 指向的 prefab 加载
- 火焰特效优先查找场景中已有的火焰粒子系统

如果资源不存在，脚本会退回到基础几何体方案：

- 蘑菇可退化成 `Capsule`
- 一些场景结构可退化为 `Cube`、`Cylinder`、`Sphere`

这让项目在资源缺失时仍然可以运行，比较适合教学和快速演示。

## 9. 当前项目的特点和优点

- 结构简单，适合初学者理解
- 玩法闭环完整，有明确开始、过程和结束
- 自定义逻辑集中，容易维护
- 自动搭场景，减少手工绑定成本
- 视觉反馈齐全，演示效果直观

## 10. 当前项目的限制

虽然这个项目已经可以正常演示，但它目前更像“功能原型”，还存在一些限制：

- 玩法只支持一个固定菜品：蘑菇汤
- 输入写死在键盘按键中，暂未抽象成可配置输入系统
- UI 也是写死生成，扩展多个菜单时会变得不方便
- 阶段和配方是硬编码，不适合直接扩展很多菜谱
- 文本直接写在代码里，不利于后期做多语言
- 使用 `UnityEngine.UI.Text`，后续更推荐迁移到 TextMeshPro
- 通过 `AssetDatabase` 加载资源的逻辑更偏编辑器原型方案，正式发布前建议检查运行时兼容性设计

## 11. 如果后续要继续扩展，建议优先做这些

### 方案一：做成多个菜谱

可以把以下内容抽成配置数据：

- 菜品名称
- 每个阶段需要的动作
- 每个阶段对应提示文本
- 食材种类和数量
- 完成后的颜色变化

这样后续就能扩展成：

- 蘑菇汤
- 鱼汤
- 烤肉
- 炖锅

### 方案二：把玩法逻辑数据化

建议把状态机中的关键参数做成 ScriptableObject，例如：

- 火力衰减速度
- 每阶段目标进度
- 食材需求
- 搅拌次数
- 完成判定

这样程序和策划配置会更容易分离。

### 方案三：优化 UI 和交互提示

可以继续补充：

- 开始界面
- 完成界面
- 失败条件
- 音效反馈
- 按键图标提示
- 更明显的阶段动画

## 12. 给接手同学的快速理解建议

如果是第一次接手这个项目，建议按下面顺序看：

1. 先打开 `Assets/Scenes/SampleScene.unity`
2. 再看 `Assets/Scripts/Cooking/MushroomSoupSceneBootstrap.cs`
3. 理解它如何创建场景对象和 UI
4. 然后看 `Assets/Scripts/Cooking/MushroomSoupGame.cs`
5. 重点理解 `SoupStage` 状态机和 `UpdateCookingProgress()`
6. 最后再根据需要调整资源路径、按键和数值参数

## 13. 一句话总结

这个项目本质上是一个基于 Unity 的“蘑菇汤制作交互演示”，用两个核心脚本完成了场景搭建、交互输入、状态推进、动画反馈和 UI 展示，结构清晰，适合课程作业展示、项目讲解和后续二次扩展。
