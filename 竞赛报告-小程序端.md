# 基于脑机接口的ADHD儿童专注力训练系统——微信小程序端设计与实现

## 参赛作品报告

- 作品 ID 号: 3999
- 参赛学生类型：本科生
- 参加赛道：赛题三
- 组别：命题项目组

---

## 内容完整性自查表

| 任务或技术指标名称 | 完成效果 | 呈现方式 |
|---|---|---|
| 1. 游戏训练数据云端采集与存储 | 已完成 | 2.3.1 云函数设计与实现 |
| 2. 训练数据可视化（专注力趋势图、ADHD四维雷达图） | 已完成 | 2.3.2 报告页面可视化设计 |
| 3. 时间胶囊首页（今日战报、关卡进度） | 已完成 | 2.3.3 时间胶囊首页设计 |
| 4. 家长/医生双角色报告系统 | 已完成 | 3.2 技术、功能指标及测试结果 |
| 5. Unity游戏端数据上报集成 | 已完成 | 2.3.4 Unity-HTTP-云函数数据链路 |

---

## 摘要

针对ADHD（注意缺陷多动障碍）儿童专注力训练需求，本项目设计并实现了一套基于脑机接口（BCI）的游戏化训练系统。本文重点阐述该系统的微信小程序端设计与实现，包括云端数据采集架构、训练数据可视化平台以及用户报告系统。

小程序端采用微信云开发架构，通过云函数 `submitGameLog` 同时支持Unity游戏HTTP调用和小程序内部调用两种数据接入方式，训练数据写入云数据库 `main_game_logs`。前端包含三个核心页面：（1）**时间胶囊首页**，展示今日专注力战报、训练时长、关卡进度等概览信息；（2）**康复报告页**，基于ECharts实现专注力趋势折线图和ADHD四维（持续注意力、选择注意力、执行功能、冲动控制）雷达图，支持家长和医生双角色切换；（3）**欢迎引导页**，提供新用户引导。

在Unity游戏端，通过 `GameLogSender` 组件实现HTTP POST请求将训练日志（专注力均值/峰值、走神次数、ADHD四维指标、EEG脑电指标等）发送至云函数。`GameplayMetricsTracker` 组件在游戏运行期间实时采集BCI注意力数据，烹饪完成时自动触发数据上报。

测试结果表明，系统能够正确完成数据从Unity游戏端到云数据库的上传、存储和小程序端可视化展示的全链路闭环，各功能指标满足设计要求。

**关键词：** 脑机接口，ADHD，专注力训练，微信小程序，云开发，数据可视化

---

## 1 作品概述

### 1.1 背景及意义

注意缺陷多动障碍（ADHD）是儿童期最常见的神经发育障碍之一，核心症状包括注意力不集中、冲动控制和执行功能缺陷。传统的药物治疗存在副作用和依从性问题，而基于脑机接口（BCI）的神经反馈训练为ADHD非药物干预提供了新的技术路径。

本项目将BCI头环设备与Unity 3D游戏结合，通过"森厨小当家"烹饪主题游戏吸引儿童参与专注力训练。游戏中的火焰控制、搅拌操作、捉鱼动作等核心玩法均需要儿童维持稳定注意力才能完成，实现了"训练即游戏"的设计理念。

微信小程序端作为整个系统的数据中枢和可视化平台，承担着训练数据采集、存储、分析和展示的关键角色。家长可通过小程序查看儿童的专注力变化趋势和训练成果；医生可基于ADHD四维评估雷达图和EEG脑电指标进行远程康复指导。这一云端数据闭环为ADHD儿童个性化康复方案制定提供了数据支撑。

### 1.2 研究基础

**硬件基础：** 系统采用HybridBCI脑机接口平台，支持通过WebSocket实时接收BCI头环采集的注意力值、眨眼检测、陀螺仪姿态等生物信号数据。

**游戏基础：** Unity端已完成"森厨小当家"烹饪游戏的场景搭建和核心玩法逻辑，包括蘑菇汤制作（Soup阶段）、河边捉鱼（FishCatch阶段）、煎鱼（FriedFish阶段）三个子关卡，以及基于BCI注意力的火焰控制和手势交互系统。

**云平台基础：** 微信云开发平台提供云函数（Cloud Functions）、云数据库（Cloud Database）和云存储等后端基础设施，免除了传统服务器运维的复杂性，适合快速原型开发和学生竞赛场景。

### 1.3 需求分析

根据ADHD训练场景和用户角色分析，小程序端需满足以下需求：

**（1）数据采集需求：** 游戏训练结束后，系统需自动将训练数据（时间戳、训练时长、平均专注力、专注力峰值、走神次数、ADHD四维评估指标、EEG脑电指标、关卡信息等）上传至云端数据库，确保数据的完整性和及时性。

**（2）家长端需求：** 家长需要直观了解儿童的训练情况，包括今日专注力战报、训练趋势图表、关卡进度等，界面应简洁友好、信息层次清晰。

**（3）医生端需求：** 医生需要查看ADHD四维专注力评估（持续注意力、选择注意力、执行功能、冲动控制）和EEG脑电指标（均值、峰值、谷值、高度专注占比等），以辅助临床诊断和康复方案调整。

**（4）系统集成需求：** Unity游戏端与小程序云函数之间需建立可靠的数据传输通道，支持HTTP协议通信，同时兼容小程序内部直接调用云函数的方式。

### 1.4 研究目标（已实现的技术、功能指标）

本作品小程序端已实现以下技术功能指标：

| 序号 | 功能指标 | 完成情况 |
|------|---------|---------|
| 1 | 云函数双通道数据接入（HTTP + 小程序直调） | ✅ 已实现 |
| 2 | 训练日志云端存储，字段包含14项核心指标 | ✅ 已实现 |
| 3 | 时间胶囊首页：今日战报、关卡进度、训练时长统计 | ✅ 已实现 |
| 4 | 康复报告页：ECharts专注力趋势图 + ADHD四维雷达图 | ✅ 已实现 |
| 5 | 家长/医生双角色切换报告系统 | ✅ 已实现 |
| 6 | Unity游戏端自动数据上报（HTTP POST） | ✅ 已实现 |
| 7 | 游戏内专注力/走神/EEG指标的实时追踪采集 | ✅ 已实现 |
| 8 | 医生评语提交与展示 | ✅ 已实现 |
| 9 | 本周数据聚合分析（总时长、均专注力、峰值、稳定率） | ✅ 已实现 |

---

## 2 作品方案设计及实现

### 2.1 作品实现的技术路线（方案）概述

系统整体采用 **"Unity游戏 → 微信云函数 → 云数据库 → 小程序前端"** 的四层架构，如图2.1所示。

```
┌─────────────┐     HTTP POST      ┌──────────────┐     add()      ┌──────────────┐
│  Unity游戏   │ ──────────────────▶│ submitGameLog │ ─────────────▶│ main_game    │
│ (C# 客户端)  │                    │   云函数      │               │ _logs 数据库  │
└─────────────┘                    └──────────────┘               └──────┬───────┘
                                         │                               │
                                         │ wx.cloud.callFunction()       │ .get()
                                         ▼                               ▼
                                  ┌──────────────┐               ┌──────────────┐
                                  │  小程序内部   │               │  小程序前端   │
                                  │  (index.ts)   │               │ (report.ts)  │
                                  └──────────────┘               └──────────────┘
```

**图2.1 系统整体架构**

技术路线核心要点：

1. **云函数层**（`submitGameLog/index.js`）：基于Node.js + wx-server-sdk，同时处理HTTP触发（Unity调用）和小程序直调两种请求方式。HTTP模式下解析 `event.body` 中的JSON数据；小程序直调模式下通过 `cloud.getWXContext()` 自动获取用户openid。数据经 `sanitize()` 函数校验和补全后写入云数据库。

2. **数据库层**：使用微信云数据库的 `main_game_logs` 集合存储训练日志，每条记录包含时间戳、游戏关卡、训练时长、专注力指标、ADHD四维评估、EEG脑电指标等14个字段。通过 `_openid` 字段实现用户数据隔离。

3. **小程序前端层**：采用微信原生框架 + TypeScript，引入ECharts组件实现数据可视化。包含三个页面：welcome（欢迎引导）、index（时间胶囊首页）、report（康复报告）。

4. **Unity客户端层**：`GameLogSender.cs` 基于 `UnityWebRequest` 实现HTTP POST请求；`GameplayMetricsTracker.cs` 在游戏运行时实时采集BCI注意力数据，烹饪完成时自动构建 `GameLogData` 对象并触发提交。

### 2.2 技术方案对比

**（1）后端方案对比**

| 对比维度 | 传统云服务器方案 | 微信云开发方案（本作品采用） |
|---------|-----------------|---------------------------|
| 开发成本 | 需搭建服务器、配置域名、管理SSL证书 | 微信平台内置，零配置 |
| 用户身份 | 需自建登录系统 | 自动获取微信openid，天然打通 |
| 数据库 | 需自建MySQL/MongoDB | 云数据库开箱即用 |
| 运维成本 | 需持续维护服务器 | 无服务器（Serverless），按量计费 |
| 适用场景 | 大型商业项目 | 竞赛原型、快速验证 |

本作品选择微信云开发方案，主要基于以下考虑：项目面向微信生态用户（家长/医生），云函数天然集成微信用户体系，免去用户登录注册的开发工作量；Serverless架构免运维，适合学生竞赛场景的快速开发和迭代。

**（2）数据可视化方案对比**

| 对比维度 | Canvas自绘方案 | ECharts方案（本作品采用） |
|---------|---------------|-------------------------|
| 开发效率 | 需手写绘图逻辑 | 声明式配置，快速出图 |
| 交互能力 | 需自行实现Tooltip等 | 内置tooltip、legend、缩放 |
| 图表丰富度 | 受限于开发量 | 内置折线图、雷达图、柱状图等 |
| 小程序兼容 | 原生Canvas API | ec-canvas组件适配 |

本作品采用ECharts + ec-canvas组件方案，实现了专注力趋势折线图与走神次数柱状图混合图表（家长端），以及ADHD四维雷达图（医生端），开发效率高且图表交互体验好。

### 2.3 作品方案设计及实现过程

#### 2.3.1 云函数设计与实现

`submitGameLog` 云函数是整个系统的数据中枢，核心设计要点如下：

**双通道接入：** 通过检测 `event.httpMethod` 判断调用来源。若存在 `httpMethod` 字段则为HTTP触发（Unity调用），解析JSON请求体；否则为小程序直调，通过 `cloud.getWXContext()` 获取用户openid。

**数据校验与补全（sanitize函数）：** 对14个字段逐一处理——
- `timestamp`：若Unity未传则取 `Date.now()`；
- `game_module`：校验是否为 `camp/room/kitchen` 之一，无效时默认 `camp`；
- 所有数值字段执行类型转换（`Number()`）和范围钳制（`clamp()`），防止异常数据污染；
- 嵌套对象（`dimensionMetrics`、`eegMetrics`）使用可选链（`?.`）安全访问。

**HTTP响应构建：** 通过 `httpResponse()` 函数统一构造 `{statusCode, headers, body}` 格式的HTTP响应，设置CORS头以支持跨域请求。

关键代码片段：
```javascript
// 双通道判断
const isHttp = !!event.httpMethod;

// HTTP模式解析
if (isHttp) {
    data = JSON.parse(event.body || '{}');
    userOpenid = data.openid || data.userId || 'unity_user';
} else {
    data = event;
    userOpenid = cloud.getWXContext().OPENID;
}

// 数据校验与写入
const record = sanitize(data);
record._openid = userOpenid;
const result = await db.collection('main_game_logs').add({ data: record });
```

#### 2.3.2 报告页面可视化设计

康复报告页（`pages/report/report.ts`）是系统的核心数据展示模块，支持家长和医生双角色切换。

**家长端图表：** 采用ECharts双Y轴混合图——折线图展示每日平均专注力趋势，柱状图叠加显示单局走神次数。图表按时间正序排列，标注专注力峰值日。X轴为日期短格式（如"6.13"），Y轴左轴0-100分专注度，右轴0-10次走神。

**医生端雷达图：** 采用ECharts雷达图展示ADHD四维评估——持续注意力（Sustained Attention）、选择注意力（Selective Attention）、执行功能（Executive Function）、冲动控制（Impulse Control）。四个维度均以0-100分标准化，圆心为正圆型雷达，紫色填充区域直观反映各维度强弱。

**数据映射逻辑：**
- 图表数据：按 `timestamp` 降序拉取本周记录，反转后映射至日期轴
- 雷达数据：取最新一条记录的 `dimensionMetrics` 四个字段
- 本周聚合：计算总训练时长、平均专注力、峰值专注力、专注稳定率（≥60分占比）
- 关卡进度：按 `game_module` 分组计算各关卡平均专注力

**本周聚合指标计算示例：**
```typescript
// 平均专注力
const avgFocus = Math.round(allAttention.reduce((a, b) => a + b, 0) / allAttention.length);

// 专注稳定率（≥60分的占比）
const stableCount = allAttention.filter(v => v >= 60).length;
const stability = Math.round((stableCount / allAttention.length) * 100);
```

#### 2.3.3 时间胶囊首页设计

时间胶囊首页（`pages/index/index.ts`）提供训练概览信息，降低家长使用门槛：

- **今日战报卡片：** 根据当日平均专注力显示对应等级（≥80 "优秀 ✨" / ≥60 "良好 👍" / <60 "需要加油 💪"）和鼓励文案
- **关卡进度指示：** 三个关卡（食材准备营/失重备菜室/林间小厨房）的完成进度环形图
- **今日统计摘要：** 今日训练总时长、专注力峰值、走神总次数
- **最后同步时间：** 标注数据刷新时间，增强可信度

数据查询策略：一次拉取最近100条记录（`orderBy('timestamp', 'desc').limit(100)`），按时间戳筛选今日数据进行前端聚合计算，避免频繁数据库查询。

#### 2.3.4 Unity-HTTP-云函数数据链路

Unity游戏端通过两个C#脚本实现数据的自动采集和上报：

**GameLogSender.cs：** 基于 `UnityWebRequest` 封装HTTP POST请求，将 `GameLogData` 对象通过 `JsonUtility.ToJson()` 序列化后发送至云函数HTTP触发地址。请求头设置 `Content-Type: application/json`，支持异步回调（`OnSubmitSuccess` / `OnSubmitFailed` 事件）。

**GameplayMetricsTracker.cs：** 游戏运行时追踪器，核心功能包括：
1. **实时注意力采样：** 每帧从 `HybridBciGameplayInput.SmoothedAttention` 读取BCI注意力值并存入 `List<float>` 样本集
2. **走神检测：** 注意力低于阈值（40分）且距上次走神超过冷却时间（3秒）计为一次走神
3. **峰值追踪：** 持续更新会话专注力峰值
4. **EEG指标累积：** 统计注意力<60和<80的时间占比、变异系数等
5. **游戏完成检测：** 通过反射读取 `MushroomSoupGame.dishPhase` 字段，检测烹饪完成事件
6. **自动提交：** 烹饪完成时自动构建 `GameLogData` 并调用 `logSender.SubmitLog()`

**GameLogData数据模型**包含14个字段，覆盖时间戳（`timestamp`）、游戏模块（`game_module`）、训练时长（`durationMinutes`）、专注力核心指标（`avgAttention`/`peakFocus`/`distractCount`）、ADHD四维（`dimensionMetrics`）、EEG脑电（`eegMetrics`）等。

**场景集成方式：** 通过修改 `MushroomSoupSceneBootstrap.cs`，在场景启动流程末尾自动创建 `GameLogSender` 和 `GameplayMetricsTracker` 两个GameObject，无需手动拖拽配置。Inspector中提供 `Cloud Logging` 配置段，可开关云日志功能、设置API地址和测试用户ID。

---

## 3 作品测试方案及测试结果

### 3.1 测试方案

测试目标为验证Unity游戏端到小程序前端的数据全链路闭环，包括以下测试用例：

| 测试编号 | 测试项目 | 测试方法 | 预期结果 |
|---------|---------|---------|---------|
| T1 | 云函数HTTP触发 | 使用Postman发送模拟训练数据 | 返回 `{code:0, msg:"ok"}`，数据库有对应记录 |
| T2 | Unity数据上报 | 运行游戏完成烹饪流程，观察Console日志 | `[GameLogSender] ✅ 提交成功` |
| T3 | 数据完整性 | 检查云数据库记录的各字段值 | 时间戳、专注力、ADHD四维等字段非零（非默认值） |
| T4 | 时间胶囊首页 | 打开小程序首页 | 显示今日战报、关卡进度、训练时长 |
| T5 | 康复报告-家长端 | 切换到家长角色 | 折线图+柱状图显示本周数据 |
| T6 | 康复报告-医生端 | 切换到医生角色 | 雷达图显示ADHD四维评估 |
| T7 | 医生评语提交 | 输入评语并提交 | `doctor_comments` 集合有记录，页面显示评语 |
| T8 | 数据查询性能 | 拉取本周30条记录 | 响应时间 < 2秒 |

### 3.2 技术、功能指标及测试结果

**测试环境：**
- Unity版本：2022.3 LTS
- 微信开发者工具：1.06.xxx
- 微信基础库版本：2.2.3+
- 云开发环境：cloud1-d9gz2tmfub107d0ff

**测试结果汇总：**

| 功能指标 | 测试结果 | 备注 |
|---------|---------|------|
| 数据上传成功率 | 100% | HTTP POST请求正常响应 |
| 数据字段完整性 | 14/14字段全部入库 | 含嵌套对象 dimensionMetrics、eegMetrics |
| 时间戳准确性 | 正确 | 云端兜底逻辑确保不丢失 |
| 专注力趋势图渲染 | 正常 | ECharts折线图+柱状图双Y轴混合 |
| 四维雷达图渲染 | 正常 | 持续/选择/执行/冲动四维度可视化 |
| 双角色切换 | 正常 | 家长↔医生一键切换 |
| 本周聚合计算 | 正常 | 总时长/均专注力/峰值/稳定率 |
| 关卡进度统计 | 正常 | camp/room/kitchen 分模块聚合 |

**云函数日志示例（T1-T2）：**
```
📦 [DEBUG] 原始请求体 event.body: {"openid":"test_user","game_module":"kitchen","avgAttention":82,"peakFocus":95,...}
📦 [DEBUG] 解析后的 data 对象: {"openid":"test_user","game_module":"kitchen","avgAttention":82,...}
📦 [DEBUG] sanitize 后的 record: {"timestamp":1718100000000,"game_module":"kitchen","avgAttention":82,...}
✅ 写入成功 _id=xxxxxx
```

**数据库记录示例（T3）：**
```json
{
  "_id": "xxxxxxxx",
  "_openid": "test_user",
  "timestamp": 1718100000000,
  "game_module": "kitchen",
  "durationMinutes": 8,
  "recipeName": "蘑菇汤+煎鱼套餐",
  "avgAttention": 82,
  "peakFocus": 95,
  "distractCount": 3,
  "dimensionMetrics": {
    "sustained": 82,
    "selective": 68,
    "executive": 87,
    "impulse": 68
  },
  "eegMetrics": {
    "meanFocus": 82.0,
    "peakFocus": 95.0,
    "valFocus": 38.0,
    "durationRatio60": 72.5,
    "durationRatio80": 35.2,
    "avgDistractDuration": 2.5,
    "focusCv": 0.28
  }
}
```

### 3.3 技术可行性分析及创新说明

**（1）技术可行性**

- **云开发Serverless架构：** 微信云函数免运维，冷启动时间<500ms，满足游戏数据上报的低延迟需求。云数据库提供自动索引和权限控制，通过 `_openid` 实现天然的租户隔离。
- **ECharts小程序适配：** ec-canvas组件为微信官方推荐的ECharts小程序适配方案，经广泛验证，渲染稳定。
- **Unity-HTTP通信：** UnityWebRequest为Unity原生HTTP模块，无需第三方库，在Unity 2019+版本中稳定可靠。
- **游戏内反射检测：** 通过C#反射读取 `MushroomSoupGame.dishPhase` 私有字段实现非侵入式的游戏完成检测，不修改游戏逻辑代码。

**（2）创新点**

1. **BCI数据→游戏→云端→可视化全链路闭环：** 与常见的仅本地展示BCI数据的方案不同，本系统实现了注意力数据从采集、游戏化训练、云端存储到多角色可视化报告的完整闭环，为ADHD远程康复提供了数据基础设施。

2. **ADHD四维专注力评估模型：** 在传统单一注意力值的基础上，结合游戏行为数据自动推算四维指标——持续注意力（基于均值）、选择注意力（基于波动标准差）、执行功能（基于任务完成度+注意力水平）、冲动控制（基于走神频率），为ADHD多维度评估提供了新思路。

3. **双角色报告系统：** 家长端侧重"今天孩子表现如何"的直观概览，医生端侧重ADHD四维雷达图和EEG指标的深度分析，同一数据源、不同视角的差异化呈现，满足不同角色的信息需求。

4. **非侵入式游戏集成：** `GameplayMetricsTracker` 通过反射读取游戏状态、自动追踪指标、自动触发上报，无需修改游戏核心逻辑代码，实现数据采集层与游戏逻辑层的松耦合。

---

## 4 总结

### 4.1 作品展示

本作品小程序端实现了以下核心功能模块：

**（1）云端数据采集架构**
- `submitGameLog` 云函数支持HTTP和小程序直调双通道
- `main_game_logs` 云数据库存储14项核心训练指标
- 完善的 `sanitize()` 数据校验和补全机制

**（2）时间胶囊首页**（家长日常视角）
- 今日专注力战报（等级+鼓励文案）
- 三个关卡进度概览
- 今日训练时长/峰值/走神统计
- 最后同步时间展示

**（3）康复报告页**（家长+医生深度分析）
- ECharts专注力趋势折线图 + 走神柱状图
- ADHD四维评估雷达图
- 本周聚合指标（总时长、均专注力、峰值、稳定率）
- 家长/医生双角色一键切换
- 医生评语提交与展示

**（4）Unity游戏端集成**
- `GameLogSender.cs`：HTTP数据上报
- `GameplayMetricsTracker.cs`：实时指标追踪与自动提交
- `MushroomSoupSceneBootstrap.cs`：场景启动时自动创建相关组件

系统UI界面遵循森林自然风格设计，主色调为 `#8FBC8F`（鼠尾草绿），底部TabBar包含"时间胶囊"和"康复报告"两个标签页，操作逻辑清晰，适合非技术背景的家长日常使用。

### 4.2 展望

1. **长周期数据分析：** 当前系统主要展示本周数据，后续可增加月度/季度趋势分析、训练效果前后对比等长周期分析功能。
2. **个性化推荐：** 基于历史训练数据，利用机器学习算法为不同ADHD亚型儿童推荐最适合的训练关卡和难度。
3. **家庭训练计划：** 增加医生下发家庭训练任务功能，家长端可打卡完成，形成"评估-训练-反馈-调整"的完整康复闭环。
4. **多人数据看板：** 医生端增加患者列表和批量数据对比功能，提升临床管理效率。
5. **数据导出：** 支持训练报告导出为PDF，方便家长与学校、医院共享。
